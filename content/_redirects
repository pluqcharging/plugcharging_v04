# this adds the redirect configuration for NETLIFY.
# _redirects must go to the public folder, therefore placed in content.
# _redirects is processed first, then the netlify.toml is applied if existing.
# Permanently (301)

/chargepointId /chargepointid 301